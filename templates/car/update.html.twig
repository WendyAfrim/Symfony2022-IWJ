{% extends 'base.html.twig' %}

{% block title %}Edition de la voiture {{ car.matriculation }}{% endblock %}

{% block body %}
    Edition de la voiture {{ car.matriculation }}:

    <br>

    {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit">Modifier</button>
    {{ form_end(form) }}
{% endblock %}
